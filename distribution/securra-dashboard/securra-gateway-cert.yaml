apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: securra-ingressgateway-certs
  namespace: istio-system
spec:
  secretName: securra-ingressgateway-certs
  issuerRef:
    name: gateways-issuer
    kind: ClusterIssuer
  commonName: dashboard.securra.beyond.ai
  dnsNames:
    - dashboard.securra.beyond.ai
